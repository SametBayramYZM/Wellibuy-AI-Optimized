# Production Environment Variables for Vercel

# Database - Use MongoDB Atlas for production
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/wellibuy

# JWT Secret - MUST be changed to a secure random string
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-production-jwt-secret-minimum-32-characters

# OpenAI API Key
OPENAI_API_KEY=sk-your-production-openai-api-key

# Server Configuration
NODE_ENV=production
PORT=3001

# Frontend URL (Vercel app URL)
FRONTEND_URL=https://your-app-name.vercel.app

# Backend URL (Your API endpoint - can be same as FRONTEND_URL)
BACKEND_URL=https://your-app-name.vercel.app

# Next.js Public Variables (visible to browser)
NEXT_PUBLIC_API_URL=https://your-app-name.vercel.app/api
